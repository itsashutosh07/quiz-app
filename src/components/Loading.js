import React from "react";

/**
 * Loading component displayed while lazy-loaded components are fetched.
 */
const Loading = () => (
  <div style={{ textAlign: "center", padding: "20px" }}>
    <p>Loading...</p>
  </div>
);

export default Loading;
